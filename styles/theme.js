import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    overcompiled: {
      100: "#123FFF"
    },
    characterSummaryBkgnd: "#F0F0F0"
  }
})

export default theme
